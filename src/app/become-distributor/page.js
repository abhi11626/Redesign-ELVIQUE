import LeadForm from "@/components/forms/LeadForm";

export default function BecomeDistributorPage() {
  return (
    <LeadForm
      title="Become A Distributor"
      description="Submit your interest and start your journey with Elvique through our structured onboarding process."
      buttonText="Submit Interest"
      showBusinessFields={true}
    />
  );
}
